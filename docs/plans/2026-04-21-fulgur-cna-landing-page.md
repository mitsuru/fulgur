# Landing Page (index.en.md / index.ja.md) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Create the fulgur.dev landing page in English and Japanese, covering Hero, Use Cases, Features, Comparison Table, Get Started, and Motivation sections.

**Architecture:** Write `website/docs/index.en.md` and `website/docs/index.ja.md` using MkDocs Material markdown extensions (tabbed, admonition, attr_list). No new files or config changes needed — the MkDocs setup (mkdocs.yml, pyproject.toml) is already in place from fulgur-35m.

**Tech Stack:** MkDocs Material 9.x, mkdocs-static-i18n (suffix structure), pymdownx.tabbed, pymdownx.emoji

---

### Task 1: Verify local build works

**Files:**

- Read: `website/mkdocs.yml`
- Read: `website/docs/index.en.md`

**Step 1: Install dependencies**

```bash
cd website && uv sync
```

Expected: resolves from `uv.lock` without errors.

**Step 2: Build the site in strict mode**

```bash
cd website && uv run mkdocs build --strict
```

Expected: `INFO - Documentation built in X.XX seconds` with no warnings or errors.

**Step 3: Verify markdownlint passes on current state**

```bash
npx markdownlint-cli2 '**/*.md'
```

Expected: no errors.

---

### Task 2: Write `index.en.md` — Hero section

**Files:**

- Modify: `website/docs/index.en.md`

**Step 1: Replace the placeholder with the Hero section**

Replace the entire content of `website/docs/index.en.md` with:

```markdown
---
hide:
  - navigation
  - toc
---

# Fulgur

**Your AI writes freely. Fulgur delivers with authority.**

An HTML/CSS to PDF library for Rust.
Seal the output your AI agent generates into authoritative, permanent documents.

[Get Started](#get-started){ .md-button .md-button--primary }
[GitHub](https://github.com/fulgur-rs/fulgur){ .md-button }

---

## Quick Start { #get-started }

=== "Rust"

    ```toml
    # Cargo.toml
    [dependencies]
    fulgur = "*"
    ```

    ```rust
    use fulgur::Engine;

    let html = "<h1>Hello, Fulgur</h1>";
    let pdf: Vec<u8> = Engine::builder().build().render_html(html)?;
    ```

=== "CLI"

    ```bash
    cargo install fulgur-cli
    fulgur render input.html -o output.pdf
    ```
```

**Step 2: Build to verify Hero renders correctly**

```bash
cd website && uv run mkdocs build --strict
```

Expected: builds without errors.

**Step 3: Run markdownlint**

```bash
npx markdownlint-cli2 'website/docs/index.en.md'
```

Expected: no errors.

---

### Task 3: Write `index.en.md` — Use Cases section

**Files:**

- Modify: `website/docs/index.en.md`

**Step 1: Append Use Cases section after the Quick Start section**

Add the following after the Quick Start section:

```markdown
---

## Use Cases

<div class="grid cards" markdown>

- :robot: **AI Agent Output**

    Seal HTML reports generated by AI agents into authoritative, permanent PDFs.

- :page_facing_up: **SaaS Invoice Generation**

    Generate invoices and receipts dynamically from templates and JSON data in multi-tenant environments.

- :bar_chart: **Batch Reports**

    Produce thousands of PDFs in parallel from JSON data with deterministic, reproducible output.

</div>
```

**Step 2: Build and verify**

```bash
cd website && uv run mkdocs build --strict
```

Expected: no errors.

---

### Task 4: Write `index.en.md` — Features section

**Files:**

- Modify: `website/docs/index.en.md`

**Step 1: Append Features section**

```markdown
---

## Features

<div class="grid cards" markdown>

- :zap: **Blazing Fast**

    Throughput that outpaces wkhtmltopdf and Puppeteer. Benchmarked, not just claimed.

- :crab: **Pure Rust**

    No headless browser, no Chromium, no C++ runtime. A single `cargo add fulgur`.

- :package: **Offline-First**

    Zero network access at render time. Every asset is explicitly bundled — perfect for sandboxed and WASM environments.

- :lock: **Deterministic Output**

    Same HTML + same assets = byte-identical PDF every time, on every machine.

- :notebook: **CSS Paged Media (GCPM)**

    Running headers/footers, page counters, named pages, and margin boxes — the CSS spec that matters for real documents.

</div>
```

**Step 2: Build and verify**

```bash
cd website && uv run mkdocs build --strict
```

Expected: no errors.

---

### Task 5: Write `index.en.md` — Comparison Table section

**Files:**

- Modify: `website/docs/index.en.md`

**Step 1: Append Comparison Table section**

```markdown
---

## Comparison

|  | Fulgur | wkhtmltopdf | WeasyPrint | fullbleed | Puppeteer | Prince | Gotenberg |
|--|--------|-------------|------------|-----------|-----------|--------|-----------|
| **Pure Rust** | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ |
| **WASM support** | planned | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **CSS GCPM depth** | ★★★ | ★★ | ★★★ | ★ | ★★ | ★★★★ | ★★ |
| **No headless browser** | ✅ | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ |
| **JavaScript execution** | ❌ | △ | ❌ | ❌ | ✅ | ✅ | ✅ |
| **Serverless-friendly** | ✅ | ✅ | ✅ | ✅ | △ | △ | △ |
| **Speed** | ⚡⚡⚡ | ⚡ | ⚡ | ⚡⚡⚡ | ⚡ | ⚡⚡ | ⚡ |
| **License** | MIT/Apache | LGPL | BSD | MIT | MIT | Commercial | MIT |
| **Install size** | small | medium | medium | small | large | medium | large |

> Prince is the reference implementation for CSS Paged Media and the closest benchmark for CSS coverage.
> Gotenberg requires Docker. Puppeteer and Gotenberg bundle a full Chromium installation.
```

**Step 2: Build and verify**

```bash
cd website && uv run mkdocs build --strict
```

Expected: no errors.

---

### Task 6: Write `index.en.md` — Motivation and Footer sections

**Files:**

- Modify: `website/docs/index.en.md`

**Step 1: Append Motivation section**

```markdown
---

## Standing on the Shoulders of wkhtmltopdf

Fulgur was inspired by [wkhtmltopdf](https://wkhtmltopdf.org/) — a tool that served
the web development community for over a decade. We built Fulgur to carry that legacy
forward in the Rust era: faster, safer, and ready for the age of AI-generated documents.

---

*MIT / Apache-2.0 &nbsp;·&nbsp; [GitHub](https://github.com/fulgur-rs/fulgur) &nbsp;·&nbsp; [crates.io](https://crates.io/crates/fulgur)*
```

**Step 2: Full build + markdownlint**

```bash
cd website && uv run mkdocs build --strict
npx markdownlint-cli2 'website/docs/index.en.md'
```

Expected: both pass with no errors.

**Step 3: Commit English landing page**

```bash
git add website/docs/index.en.md
git commit -m "feat(website): add English landing page (index.en.md)"
```

---

### Task 7: Write `index.ja.md` — Full Japanese translation

**Files:**

- Modify: `website/docs/index.ja.md`

**Step 1: Replace placeholder with full Japanese content**

Replace the entire content of `website/docs/index.ja.md` with the Japanese translation:

```markdown
---
hide:
  - navigation
  - toc
---

# Fulgur

**AIは自由に書く。Fulgurは権威をもって届ける。**

RustのためのHTML/CSS → PDF変換ライブラリ。
AIエージェントが生成した出力を、権威ある永続的な文書として封印する。

[はじめる](#get-started){ .md-button .md-button--primary }
[GitHub](https://github.com/fulgur-rs/fulgur){ .md-button }

---

## クイックスタート { #get-started }

=== "Rust"

    ```toml
    # Cargo.toml
    [dependencies]
    fulgur = "*"
    ```

    ```rust
    use fulgur::Engine;

    let html = "<h1>こんにちは、Fulgur</h1>";
    let pdf: Vec<u8> = Engine::builder().build().render_html(html)?;
    ```

=== "CLI"

    ```bash
    cargo install fulgur-cli
    fulgur render input.html -o output.pdf
    ```

---

## ユースケース

<div class="grid cards" markdown>

- :robot: **AIエージェントの出力**

    AIエージェントが生成したHTMLレポートを、権威ある永続的なPDFとして封印する。

- :page_facing_up: **SaaS帳票生成**

    マルチテナント環境で、テンプレートとJSONデータから請求書・帳票を動的に生成する。

- :bar_chart: **バッチレポート**

    JSONデータから数千枚のPDFを並列生成。決定論的で再現性のある出力。

</div>

---

## 特徴

<div class="grid cards" markdown>

- :zap: **圧倒的な速度**

    wkhtmltopdfやPuppeteerを上回るスループット。計測済み、主張だけではない。

- :crab: **Pure Rust**

    ヘッドレスブラウザなし、Chromiumなし、C++ランタイムなし。`cargo add fulgur` の一行で完結。

- :package: **オフラインファースト**

    レンダリング時のネットワークアクセスはゼロ。すべてのアセットを明示的にバンドル。サンドボックスやWASM環境に最適。

- :lock: **決定論的な出力**

    同じHTML + 同じアセット = 毎回・どの環境でも同一バイトのPDF。

- :notebook: **CSS Paged Media (GCPM)**

    ランニングヘッダー/フッター、ページカウンター、名前付きページ、マージンボックス——本物の文書に必要なCSSスペック。

</div>

---

## 比較

|  | Fulgur | wkhtmltopdf | WeasyPrint | fullbleed | Puppeteer | Prince | Gotenberg |
|--|--------|-------------|------------|-----------|-----------|--------|-----------|
| **Pure Rust** | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ |
| **WASM対応** | 計画中 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **CSS GCPM深度** | ★★★ | ★★ | ★★★ | ★ | ★★ | ★★★★ | ★★ |
| **ヘッドレスブラウザ不要** | ✅ | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ |
| **JavaScript実行** | ❌ | △ | ❌ | ❌ | ✅ | ✅ | ✅ |
| **サーバーレス対応** | ✅ | ✅ | ✅ | ✅ | △ | △ | △ |
| **速度** | ⚡⚡⚡ | ⚡ | ⚡ | ⚡⚡⚡ | ⚡ | ⚡⚡ | ⚡ |
| **ライセンス** | MIT/Apache | LGPL | BSD | MIT | MIT | 商用 | MIT |
| **インストールサイズ** | 小 | 中 | 中 | 小 | 大 | 中 | 大 |

> PrinceはCSS Paged Mediaのリファレンス実装であり、CSS対応品質の最重要ベンチマーク。
> GotenbergはDockerが必要。PuppeteerとGotenbergはChromiumをフルインストール。

---

## wkhtmltopdfの遺志を継いで

Fulgurは[wkhtmltopdf](https://wkhtmltopdf.org/)に触発されたプロジェクトです——
10年以上にわたってウェブ開発コミュニティを支えてきたツールへの敬意から生まれました。
私たちはその遺産をRust時代へと引き継ぐためにFulgurを作りました:
より速く、より安全に、AI生成文書の時代に対応して。

---

*MIT / Apache-2.0 &nbsp;·&nbsp; [GitHub](https://github.com/fulgur-rs/fulgur) &nbsp;·&nbsp; [crates.io](https://crates.io/crates/fulgur)*
```

**Step 2: Full build + markdownlint**

```bash
cd website && uv run mkdocs build --strict
npx markdownlint-cli2 'website/docs/index.ja.md'
```

Expected: both pass with no errors.

**Step 3: Commit Japanese landing page**

```bash
git add website/docs/index.ja.md
git commit -m "feat(website): add Japanese landing page (index.ja.md)"
```

---

### Task 8: Final validation

**Files:**

- Read: `website/docs/index.en.md`
- Read: `website/docs/index.ja.md`

**Step 1: Full build with strict mode**

```bash
cd website && uv run mkdocs build --strict 2>&1
```

Expected: no warnings, no errors.

**Step 2: Full markdownlint check**

```bash
npx markdownlint-cli2 '**/*.md'
```

Expected: no errors.

**Step 3: Verify Rust tests still pass**

```bash
cargo test -p fulgur --lib
```

Expected: all tests pass (website changes have no effect on Rust).

**Step 4: Final commit if any fixes needed**

```bash
git add -A
git commit -m "fix(website): markdownlint and build fixes"
```
